#include<stdio.h>

int main()
{
    //Define the variables
    float feh=33, cel;
    //Conver the value of the temp from f to c, you need to put the .0 in order to define the numbers as floats instead of intergers
    cel=(feh-32)*(5.0/9.0);

    return(0);

}